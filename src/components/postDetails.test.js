import { render} from '@testing-library/react';
import PostDetails from './postDetails';

// test to make sure the details of the posts are renders
test('renders the post details', () => {
  render(PostDetails);
  
});